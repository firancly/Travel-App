# Cloudflare Worker URL for AI itinerary generation.
# This is PUBLIC (safe to ship in the app) — the Gemini key stays on the Worker.
# Copy to `.env` and set your deployed URL. Without it, the app uses the sample plan.
EXPO_PUBLIC_ITINERARY_API=https://tournet-itinerary.<you>.workers.dev
