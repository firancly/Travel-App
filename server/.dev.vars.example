# Copy to `.dev.vars` for local `wrangler dev` (this file is an example — do NOT put real keys here).
# .dev.vars is gitignored; get a free Groq key (no card) at https://console.groq.com → API Keys.
GROQ_API_KEY=gsk_your-groq-key
GROQ_MODEL=llama-3.3-70b-versatile
